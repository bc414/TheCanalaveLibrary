@namespace TheCanalaveLibrary.Server
@using TheCanalaveLibrary.SharedUI
@inherits LayoutComponentBase

@* #blazor-error-ui moved to App.razor (WU-ErrorHandling) — this layout only hosts Identity
   Manage pages, so a bar here left every interactive page with no teardown surface. *@
<div class="sticky top-0 z-(--z-sticky) flex items-center justify-end gap-4 border-b border-(--color-border) bg-(--color-surface-raised) px-4 py-3 shadow-subtle">
    <LoginDisplay />
</div>
@Body