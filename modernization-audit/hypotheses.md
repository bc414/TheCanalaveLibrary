# Hypothesis List / Coverage Matrix

Single writer: main session. Slice agents report per-hypothesis results INSIDE their findings
file; the main session transcribes here. Cell values: `clean` | MA-ids | `n/a`.
S0 (atoms) checked in Part 1; S1 (Foundation) transcribed 2026-07-17.

### H-01: `@key` on stateful list children
origin: WU-ComponentSoundness F2/F3 (StoryDeck/_localState corruption; CommentSection spoiler bleed)
check: every `@foreach` rendering a component — if the child caches a param into a private field with an `is null` guard, holds reveal/menu flags, or owns a per-row CTS, the loop MUST carry `@key` on a stable domain id
results: S0[clean] S1[clean] S2[clean — StoryDeck @key errorboundary/StoryCard, ArcManagerPanel @key rows, PropertiesForm TagSelector keys] S3[clean — ChapterManagerPanel @key draggable rows; ChapterList read-toggle state lives in parent dict, not child field] S4[clean — ExploreTab/ManualTreeCanvas keyed; TagFilter @key re-seed generation idiom correct] S5[clean — CommentSection keys root+reply CommentItem on CommentId (WU-CompSoundness F3 fix); RecommendationSection keyed] S6[clean — NotificationItem recomputes _composed unconditionally (self-healing, no @key needed); profile tabs delegate to keyed StoryDeck] S7[clean — BlogPostPage keys PollView; mod table rows hold no per-row stateful child (state in parent _activeReport); Spotlight blocks keyed]

### H-02: route-param dispatcher reload discipline
origin: WU-ComponentSoundness F1 (six dispatchers reloaded only in OnInitializedAsync)
check: every `@page` with a route param — guarded `OnParametersSetAsync` reload (sentinel + `_initialized`), plain assignment (not `??=`) for `[PersistentState]` fields on param change, transient UI state reset before same-template `NavigateTo`
results: S0[n/a] S1[n/a] S2[clean — all dispatchers guarded sentinel + _initialized, plain-assign on reload] S3[clean — ChapterReadingPage + ChapterEditorPage guarded sentinel, plain-assign PersistentState on reload, ??= only on initialLoad] S4[clean — Bookshelves/TreeSearch/CustomList dispatchers all sentinel-guarded, plain-assign reload; Bookshelves = first clean Nav.NotFound() on bad slug] S5[clean — MessagesPage sentinel + compose-modal-reset-before-navigate present; GroupPage guarded, plain-assign reload] S6[clean — ProfilePage textbook two-param guarded dispatcher (UserId+Tab sentinels, ClearTabPayload, plain-assign); Settings/Notifications no route param] S7[clean — BlogPostPage textbook guarded dispatcher; mod/Spotlight pages desktop-only no route param (mod convention)]

### H-03: unnamed `HasIndex` overwrite
origin: WU-L6 (seven USI indexes silently collapsed to one)
check: in `Server/Data/Configurations/` — any two `HasIndex` calls on the same property set without the name argument
results: S0[n/a] S1[MA-103 — unnamed unique NormalizedEmail merged into Identity's EmailIndex; original USI incident confirmed FIXED; no other dupes across 20 config files] S2[n/a — configs are S1's; no HasIndex in slice product code] S3[n/a — ChapterConfigurations are S1's; no HasIndex in slice product code] S4[n/a — EF configs S1's; raw-SQL mart indexes each explicitly+uniquely named] S5[n/a — EF configs S1's; no HasIndex in slice product code] S6[n/a — EF configs S1's; phantom ix_asp_net_roles_user_id is MA-102's shadow FK, different mechanism] S7[n/a — EF configs S1's; no HasIndex in slice product code]

### H-04: read-context concurrency (factory-per-method)
origin: WU-BrowserPass (circuit-scoped read context crash)
check: every read service creates its context per method via `IDbContextFactory`; no service/component holds a shared `ReadOnlyApplicationDbContext` field; layout-chrome-reachable services comply
results: S0[n/a] S1[clean — Theme/SiteSettings read svcs factory-per-method; TestAppFactory mirrors prod] S2[clean — all 4 read svcs factory-per-method; write svcs hold only writeDb; CS9107 protected-property idiom] S3[clean — ChapterReadService factory-per-method (7 methods); flusher opens fresh scope per flush, never captures scoped ctx in singleton] S4[clean — all read svcs + raw-ADO tree/co-occurrence svcs open ctx per method; DiscoveryMartRebuilder holds writeDb but is scoped worker, not circuit-reachable] S5[clean — all 5 read svcs factory-per-method; MessagesNavLink (layout chrome) covered by the concurrency fix] S6[clean — all read svcs factory-per-method; ClaimsPrincipalFactory injects readDb directly (sanctioned sign-in-scope exception)] S7[clean — all 5 read svcs factory-per-method; derived-write CS9107 protected-property idiom correct]

### H-05: dead Tailwind classes (bare-name / bracket-form / raw palette)
origin: 987-class bracket sweep (2026-07-01) + 49-usage bare-name sweep (2026-07-10)
check: multi-word tokens in bare form; `-[--token]` v3 syntax; raw palette (`bg-yellow-50`) or hex outside tokens. Also note any file the CI token checker apparently exempts (MA-009's DevLoginBar question)
results: S0[MA-009, MA-006] S1[clean in code; RESOLVES S0's open Q — check-design-tokens.ps1:19-23 deliberately exempts DevLoginBar/DesignGallery/ContentSurface, so MA-009 + MA-006 are sanctioned exemptions, NOT checker blind spots → downgrade both] S2[clean — paren-form throughout; bare-name semantic tokens (bg-surface etc, 108 uses codebase-wide) are sanctioned dual style, not dead] S3[MA-305 — ImportReviewPanel bg-danger (bare) + text-white (raw palette); pre-existing CI-flagged] S4[clean — paren-form throughout; text-white only on colored Control grounds (CI-green)] S5[clean — paren-form throughout; noted intra-file paren-vs-bare inconsistency in CommentItem (same color), not filed] S6[clean — paren-form throughout; token opacity modifiers valid; no bare-multiword/bracket/raw-palette] S7[clean — paren-form throughout; bare-name semantic tokens are sanctioned dual style (CI-green); no bracket/raw-palette/hex]

### H-06: unregistered silent catches
origin: logging.md registry contract (one sanctioned site); slice 0 found two violations
check: every `catch` with an empty/comment-only body — must log Warning+, rethrow/translate, or carry `// sanctioned-silent:` + a registry entry
results: S0[MA-001] S1[clean — seeder catch logs Error; upload/endpoint catches rethrow/translate; TestAppFactory.Dispose IOException swallow is test-infra, outside sweep scope] S2[MA-206 — StoryPage:225 bare catch{} on view-ping dispose JS call] S3[MA-303 — ChapterReadingPage:375 bare catch{}; import readers correctly catch→ImportException] S4[clean — no bare catch; CoOccurrence catches typed PostgresException+LogWarning (graceful degraded read); Rebuilder telemetry+rethrow] S5[MA-503 — MessagesNavLink:59 + RecommendationEditor:105 bare catch; all best-effort notify catches correctly LogWarning] S6[clean — the one silent catch (ServerActiveUserContext) is registered/annotated; best-effort blob-delete LogWarnings] S7[MA-704 — ReportDialog catch-all swallows without logging; all service notify catches correctly LogWarning/LogError]

### H-07: stale/untracked TODO(WU-x) comments in Stage-5 code
origin: `ServerCommentWriteService.cs:65` — "TODO(WU22): notify story author of new comment" in a Stage-5 cell
check: every `TODO`/`HACK`/`FIXME` — is the referenced WU done (stale), pending (tracked?), or unowned (silent gap)? Verify whether comment notifications were ever wired
results: S0[clean] S1[MA-115 (WU12 "throwaway" dev endpoints now load-bearing), MA-116 (TODO(user) default-matrix vs "seed authoritative")] S2[clean — no TODO/HACK/FIXME; ChapterNames "will be removed" note filed as dead code MA-204] S3[clean — no TODO/HACK/FIXME; doc-staleness is H-11 not a TODO] S4[clean — no TODO/HACK/FIXME in slice product code] S5[MA-506 — calibration's H-07 origin site CONFIRMED: comment-author notifications still unwired behind stale TODO(WU22)/(WU33)] S6[MA-605 — ClaimsPrincipalFactory WU30 RefreshSignInAsync handoff unclosed; WU35 notification-enrichment TODO now RESOLVED (two-pass batch implemented)] S7[MA-709 — TODO(WU33) follower-notification gap in a Stage-5 blog write cell]

### H-08: entity-not-found divergence (`Nav.NotFound()` vs manual `/not-found` nav)
origin: StoryPage.razor:138 uses `NavigationManager.NavigateTo("/not-found")` — returns 200 + client redirect, not a 404; render-and-layout.md mandates `NavigationManager.NotFound()`; SEO impact for deleted-entity URLs (F64)
check: every dispatcher's missing-entity branch — which mechanism; flag both sides (direction undetermined)
results: S0[n/a] S1[n/a — no dispatchers in slice; /not-found page itself is conformant as re-execute target] S2[**MA-202** — RESOLVED, broader than StoryPage: 5 sites NavigateTo("/not-found"), zero uses of Nav.NotFound() → deleted-entity URLs return 200] S3[MA-304 — ChapterReadingPage + ChapterEditorPage (×3) NavigateTo("/not-found"); extends MA-202 into Chapters] S4[MA-404 — TreeSearchPage inline root-not-found; BUT BookshelvesPage is the audit's FIRST clean Nav.NotFound() use] S5[mostly clean — MessagesPage first clean Nav.NotFound() in a DISPATCHER; GroupPage inline = deliberate missing-vs-hidden-Mature ambiguity (not filed, S4 precedent)] S6[MA-606 — ProfilePage NavigateTo("/not-found") ×2; zero Nav.NotFound() in slice] S7[MA-708 — BlogPostEditorPage NavigateTo("/not-found"); BlogPostPage inline soft message; zero Nav.NotFound()]

### H-09: dispatcher load parallelism (sequential awaits vs sanctioned Task.WhenAll)
origin: StoryPage.razor:142-150 — 5+ sequential awaits (under WASM: sequential HTTP round trips); layer2 sanctions `Task.WhenAll`
check: every dispatcher's load body — count independent sequential awaits; note which pages parallelize (documented: ChapterReadingPage, SettingsPage, GroupPage, NotificationsPage) vs not
results: S0[n/a] S1[n/a — HomePage does no data loads] S2[**MA-203** — RESOLVED: StoryPage runs 6 independent awaits sequentially in both lifecycle methods; Task.WhenAll not applied though layer2 sanctions it] S3[clean — ChapterReadingPage Task.WhenAlls its 4 loads (in the sanctioned list); ChapterEditorPage awaits are a genuine dependency chain] S4[clean — dispatcher loads are genuine dependency chains (candidateIds→listings→states), no StoryPage-style parallelizable set] S5[clean — GroupPage Task.WhenAlls role/stories/blog (in sanctioned set); MessagesPage awaits are a dependency chain] S6[clean — SettingsPage Task.WhenAlls 4 loads (sanctioned); ProfilePage loads are dependency chains; NotificationBellInner parallel under factory rule] S7[clean — BlogPostPage Task.WhenAlls 2 independent loads; mod pages single loads; SpotlightRedemption dependency-ordered]

### H-10: debounced/pending writes lost on dispose or navigation
origin: UserStoryInteractionPanel — dispose during the 2s debounce window may cancel the pending flush
check: every debounced/deferred write path — does disposal flush, drop, or race? Is the loss window documented as accepted?
results: S0[clean — DraftAutosave keeps localStorage backup] S1[n/a — signal buffers live in S2/S3] S2[n/a — ViewCount has no per-component debounce (synchronous buffer write, global flush + drain-after-cancel); StoryPage dispose only tears down JS ping] S3[clean/honored — reading-progress loss-tolerant-by-contract (documented); synchronous buffer write, no timer; read-mark is durable-direct + Discards in-flight pings before saving] S4[**MA-401 — RESOLVED: DROP.** UserStoryInteractionPanel.Dispose() cancels the debounce CTS and never flushes; toggle-then-navigate within 2s silently loses durable interaction intent; not documented as accepted] S5[clean/n-a — no per-component debounce; like paths write synchronously (await+reconcile/rollback); MA-401 class does not recur] S6[clean — no per-component debounce; settings save on click, mark-read immediate] S7[n/a — no per-component debounce; poll vote/blog like/redeem write synchronously (await+reconcile/rollback); MA-401 class does not recur]

### H-11: doc-vs-code staleness (post-Global-Flip class)
origin: four instances in Part 1 (MA-004, MA-010, MA-013, content-safety/BaseBlogPost)
check: any convention-doc claim contradicted by the code in your slice — FLAG for the Bucket B/doc pass, do not rule
results: S0[MA-004, MA-010, MA-013, +content-safety/BaseBlogPost] S1[MA-104 (default-deny never impl'd), MA-108 (SiteConstants nesting), MA-114 (5-instance bundle: DevDiag home path, CSP font-src, ServerSpriteReadService gone, SiteConstants keys moved, Retry-After header), MA-118 (StoryLineage member names); reconfirmed BaseBlogPost/IsTakenDown contradiction w/ fresh evidence] S2[clean for new instances; reconfirms MA-118 in-code (StoryLineage.RelationshipTypeId member drift persists)] S3[feeds MA-301 — audit/Chapters.md + ChapterManagerPanel header assert author-gating the code does for only 2 of 7 write methods; no new standalone doc-staleness] S4[mostly clean — WU44 ResultsFilterPanel flag filed as code bug MA-402 not doc; minor stale TagFilter "six tag types" comment (five correct post-WU37), noted] S5[clean for new — no doc claim contradicted beyond filed items; MA-506's stale TODO is a code marker not a doc claim] S6[MA-604 (SettingsPage injects IActiveUserContext in SharedUI — MA-004 recurrence), MA-605 (WU30 handoff), MA-610 (Identity scaffold for unbuilt features); namespace drift + PasskeySubmit asset path both RESOLVED] S7[clean for new — no post-flip doc claim contradicted beyond filed items; settled BlogPost/Poll/Spotlight notes all match code]

### H-12: fire-and-forget without exception observation
origin: slice 0 — MA-002 (autosave loop), MA-011 (toast); sanctioned: UserActivityTracker ping
check: every `_ = SomeAsync(...)` / unawaited task — is failure observed (log/restore) or can it die silently? Is loss documented as by-contract?
results: S0[MA-002, MA-011] S1[clean — no unawaited launches in slice product code] S2[clean — ViewCount client call is awaited; no _ = launches] S3[MA-306 — ChapterReadingPage:327 `_ = RecordAttributionSourceAsync(...)` fire-and-forget; other client calls awaited] S4[clean — no _ = launches; all dispatchers + tree tabs await service calls] S5[clean — no _ = launches; MessagesNavLink async-void is the sanctioned Blazor event-handler pattern] S6[clean — no _ =/unawaited launches; all client calls awaited; best-effort notify catches LogWarning] S7[clean — no _ = launches; all client calls awaited; best-effort notify catches log]

### H-13: denormalized counter discipline
origin: layer2 counter rules (atomic ExecuteUpdateAsync; transition-delta for boolean toggles; recompute mirrors wired formula)
check: every counter mutation — no tracked `++`; toggles use flip-check; missing-row no-op acknowledged
results: S0[n/a] S1[n/a — DataSeeder sets counters by construction] S2[clean — StoriesWritten uses atomic ExecuteUpdateAsync, no tracked ++] S3[clean — WordsWritten atomic ExecuteUpdateAsync; noted borderline: VersionCount++ tracked but single-author-serialized (no concurrency window)] S4[clean — USI write uses transition-delta flip-check + atomic ExecuteUpdateAsync for all 4 stat counters, no tracked ++] S5[MA-502 — RecordSuccessAsync SuccessfulRecCount++ tracked (non-atomic) in multi-reader path, lost-update race; all other counters atomic] S6[clean in slice — Badge DisplayOrder is curation not counter; UserStatRecalculator set-based two-pass; real-time increments are other slices' (S5's MA-502 the race)] S7[MA-705 — BlogPost LikeCount written as C#-computed absolute (non-atomic, MA-502 sibling); AdjustActiveReportCount + BlogPostsWritten correctly atomic]

### H-14: elevated reads annotated + named
origin: content-safety.md — `IgnoreQueryFilters` must name filters and carry `// elevated read:`
check: every `IgnoreQueryFilters` call — named-filter list form, annotation present, justification sensible
results: S0[n/a] S1[n/a — zero IgnoreQueryFilters in slice] S2[clean — 2 elevated reads (GetStoryIdsByAuthor, lineage manage) both named-filter-list + "// elevated read:" annotated] S3[n/a — zero IgnoreQueryFilters; GetChapterForEditAsync deliberately leaves Story filter active] S4[n/a — no IgnoreQueryFilters; CloneListAsync deliberately clones THROUGH the filtered read ctx (privacy-on-clone rule, verified applied)] S5[n/a — no IgnoreQueryFilters; group-rating waterfall tier-1 relies on named filters active on the read path] S6[n/a — zero IgnoreQueryFilters; write-path existence checks read unfiltered writeDb] S7[clean — ServerModerationReadService uses IgnoreQueryFilters(["IsTakenDown"]) + "// elevated read:" on all ~6 mod-queue paths; ContentRating/GroupAudience left live]

### H-15: write-path by-id lookups bypass ContentRating
origin: layer2 — omitting `IgnoreQueryFilters(["ContentRating"])` on write-path PK fetches throws phantom KeyNotFound for M-rated entities
check: write-service reads through readDb by PK — bypass present where user-setting-independent existence matters
results: S0[n/a] S1[n/a — no write-service PK reads through readDb; write-context-unfiltered pinned by ContentRatingFilterTests] S2[clean by construction — every write-service existence check reads unfiltered writeDb; no readDb PK fetch in a write path] S3[clean by construction — every write-service existence check reads unfiltered writeDb; no readDb PK fetch in a write path] S4[clean by construction — write existence checks read unfiltered writeDb; USI writes load by composite PK] S5[clean by construction — every write existence check reads unfiltered writeDb; GroupWriteService JoinAsync deliberately keeps audience filter (can't join what you can't see)] S6[clean by construction — ServerUserSettingsService reads unfiltered writeDb; no readDb PK fetch in a write path] S7[clean by construction — spotlight/poll/blog/moderation write existence checks all read unfiltered writeDb; no readDb PK fetch in a write path]

### H-16: `[FromQuery]` on non-GET array/collection params
origin: two live failure modes (startup crash with body-param sibling; per-request 400 on bare POST array — PollEndpoints vote bug)
check: every MapPost/MapPut/MapDelete handler with an array/collection param — explicit `[FromQuery]` where query-bound
results: S0[n/a] S1[clean — no array params on slice endpoints; SiteSettings scalar body carries explicit [FromBody]] S2[clean — StoryEndpoints /query + /filter-candidates carry explicit [FromQuery] int[] w/ trap comment] S3[clean — no array params on slice POST/PUT; import binds IFormFile multipart + scalar enum; progress/read-mark use scalar query params] S4[clean — GET repeated-key int[] (bindable exception); complex/array reads correctly POST (TreeSearch, CoOccurrence, ManualTree neighbors) with doc comments] S5[clean — no array params on write endpoints; bare-string body params carry [FromBody] with trap comments] S6[clean — BadgeEndpoints /display-order uses [FromBody] List<string> (avoids trap); notification/settings use scalar params] S7[clean — PollEndpoints vote carries explicit [FromQuery] int[] optionIds w/ trap comment (Global-Flip fix); all others scalar/DTO-body]

### H-17: nullable-return client reads use the empty-body-tolerant helpers
origin: Global Flip wave (empty 200 body for null → JsonException crashed StoryPage)
check: every `Task<T?>` client method — `GetNullableFromJsonAsync`/`ReadNullableFromJsonAsync`, never plain `GetFromJsonAsync`
results: S0[n/a] S1[n/a for call sites (none in slice); helpers exist + match contract; direct-test gap = MA-121] S2[clean — Client Story/Series nullable reads use GetNullableFromJsonAsync; lists use ?? []] S3[clean — ChapterReadService 3 nullable reads use GetNullableFromJsonAsync; lists ?? []; server returns Results.Json(nullable)] S4[clean — CustomList/SavedTagSelection detail reads use GetNullableFromJsonAsync (unit-covered); non-null tree DTOs use ReadFromJsonAsync+EnsureSuccess] S5[clean — every Task<T?> client read uses GetNullableFromJsonAsync; non-null shapes use plain GetFromJsonAsync/?? []] S6[clean — ClientUserProfileReadService uses GetNullableFromJsonAsync for ProfileHeaderDto?/string?; non-null shapes use plain GetFromJsonAsync] S7[clean — ClientBlogPostReadService GetById/GetForEdit use GetNullableFromJsonAsync; paged shapes use PagedResult<T>; writes use ThrowIfWriteFailedAsync]

### H-18: aria-labels on icon-only controls + EditorView-adjacent buttons
origin: testing.md collision rule; a11y Stage-5 criterion (2026-07-15)
check: icon-only buttons carry aria-label; every button in a component wrapping EditorView carries a unique aria-label
results: S0[clean] S1[clean — UserCard caret, ToastHost dismiss, Pagination prev/next all labeled+tested] S2[MA-212 — StoryPropertiesForm (wraps EditorView) submit + "+ Add link" buttons lack aria-label] S3[MA-307 — ChapterPropertiesForm submit (wraps 3 EditorViews) + ChapterList read-toggle lack aria-labels] S4[clean — no EditorView-wrapping component (plain inputs); icon-only controls (drawer close, zoom, panel close) all labeled] S5[clean — Comment/Recommendation/Message editor Save/Cancel labeled (collision rule); all icon-only controls labeled] S6[MA-607 — ProfileSettingsForm's EditorView-adjacent Save button lacks aria-label; NotificationBell/Item labeled] S7[MA-707 — BlogPostPropertiesForm submit (wraps EditorView) lacks aria-label; PollEditorForm remove-option IS labeled (wraps no EditorView)]

### H-19: AuthorizeView-gated DI leaves use the wrapper/inner split
origin: NotificationBell anonymous-401 crash (WU43 class)
check: any component whose services are only meaningful when authorized — `@inject` must live in an inner component inside `<Authorized>`
results: S0[clean — UserMenu/CreateMenu inject nothing gated] S1[n/a — no auth-gated service-injecting components in slice] S2[clean — DI-consuming edit pages are page-level [Authorize] (router never constructs for anon); no gated DI leaf] S3[clean — editor pages under page-level [Authorize]; ChapterList injects write svc but only calls it under parent CanMarkRead gate, no anon-construction crash] S4[MA-403 — AddToCustomListMenu injects at file scope under inline AuthorizeView (missing WU43 wrapper/inner split); sibling SavedTagSelectionLoadFlyout does it right] S5[clean — MessagesNavLink layout-chrome catch tolerates anon 401 (badge stays 0); no leaf matches the WU43 failure shape] S6[clean — CONFIRMED REFERENCE: NotificationBell = thin AuthorizeView wrapper, NotificationBellInner holds @inject + fetch (the WU43 reference impl)] S7[clean — no AuthorizeView-gated DI leaf; PollView/CommunitySpotlightDisplay use page-resolved CurrentUserId (not IActiveUserContext); mod pages page-level [Authorize(Roles)]]

### H-20: feedback-channel discipline
origin: error-handling.md — validation → InlineAlert; toasts only for system events; raw `ex.Message` in UI is a defect
check: forms use InlineAlert (no hand-rolled danger divs); no toast carries validation; every catch surfacing to UI goes through ExceptionPresenter
results: S0[clean] S1[clean/n/a — no forms in slice; Error.razor is a separate template-debris issue = MA-110] S2[MA-205 — SeriesCreateEditPage + MyStoryLineagesPage hand-roll validation <div> instead of InlineAlert (siblings use it correctly)] S3[clean — all chapter/import surfaces use InlineAlert; editor catches route through ExceptionPresenter; no hand-rolled alert divs] S4[MA-405 — TagEditorForm hand-rolls <p role="alert">; everything else uses InlineAlert + ExceptionPresenter] S5[MA-501+MA-504 — RecommendationSection (raw ex.Message + hand-rolled <p> + no log), MessageThread/ComposeModal/GroupCreateEditPage hand-roll danger markup; CommentSection is the in-slice reference] S6[MA-603 — SettingsPage hand-rolls success/error <div> + raw ex.Message; ProfilePage copy-handler is the in-slice reference] S7[MA-703+MA-704 — mod pages + BlogPostPage raw ex.Message via hand-rolled <p>; ReportDialog unlogged catch-all; PollView/PollsPage/SpotlightRedemption/ModSpotlight are references]
