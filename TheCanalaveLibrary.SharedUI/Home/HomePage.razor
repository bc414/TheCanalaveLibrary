@page "/"
@namespace TheCanalaveLibrary.SharedUI

@* The Community Spotlight section is live (WU-Spotlight, Feature 55); the rest of the Home
   page (discovery surface) remains a future work-unit — WU-Home, gated by middle_plan_v2
   decision row 2's remaining half. Single responsive tree (WU-ResponsiveMerge). *@

<PageTitle>Home</PageTitle>

<DevLoginBar />

<div class="flex flex-col gap-5 p-4 md:gap-6 md:p-8">
    <div class="text-center text-(--color-text-muted)">
        <h1 class="mb-3 inline-block rounded-md border border-(--color-border) bg-(--color-surface-raised) px-4 py-1.5 font-display text-xl font-semibold text-(--color-text) md:mb-4">The Canalave Library</h1>
        <p class="text-sm">Use the navigation above to explore stories, profiles, and groups.</p>
    </div>

    <div class="mx-auto w-full max-w-5xl">
        <CommunitySpotlightDisplay />
    </div>
</div>
