# Audit — Spotlight/

**Feature:** 55 (community spotlight). Community donations tied to Spotlight (§5.26). **TBD throughout.**

## Shared Context
**Entity:** `CommunitySpotlight` (Core/Models/) — exists as a placeholder POCO (`SponsoringUserId`
SetNull, `StoryId` Cascade, `DateCreated` default, `EndDate`). Donation infrastructure schema is not
designed. No services or components built.

## Feature 55 — Community Spotlight
- **L1 — Stage 1 (conceptual, §5.26).** The `CommunitySpotlight` entity is a stub; the donation/payment
  schema (provider integration, ledger, Patron-badge linkage) is undecided. This is the foundational gap —
  resolve intent in chat before any Layer-1 commitment.
- **L2 — Stage 1.** **L3-Logic — Stage 1. L3.5-Structure — Stage 1. L4 — Stage 1** (blocked + undefined).
- **L5–L8 — N/A** (nothing concrete enough to classify).

This is the least-defined cluster in the project. Everything waits on the donation-infrastructure
decision.
